'use client';

// The routing logic is now handled in /components/auth/AuthGuard.tsx
// which checks authentication and onboarding states to perform the redirection.
export default function RootPage() {
  return null;
}
